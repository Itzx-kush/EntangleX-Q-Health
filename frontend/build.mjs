import esbuild from "esbuild";import {appendFileSync,cpSync,existsSync,mkdirSync,readFileSync} from "node:fs";
const watch=process.argv.includes("--watch");mkdirSync("dist",{recursive:true});cpSync("index.html","dist/index.html");cpSync("src/styles.css","dist/styles.css");appendFileSync("dist/styles.css",readFileSync("src/phase12-ui.css"));cpSync("src/base.css","dist/base.css");if(existsSync("public"))cpSync("public","dist",{recursive:true});const options={entryPoints:["src/App.tsx"],bundle:true,minify:!watch,sourcemap:watch,outfile:"dist/app.js",format:"esm",jsx:"automatic",define:{"process.env.NODE_ENV":JSON.stringify(watch?"development":"production")},logLevel:"info"};if(watch){const ctx=await esbuild.context(options);await ctx.watch();console.log("Watching frontend sources");}else await esbuild.build(options);

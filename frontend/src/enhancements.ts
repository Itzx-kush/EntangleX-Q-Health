// @ts-nocheck
const API=globalThis.ENTANGLEX_API_URL||('http://'+(globalThis.location?.hostname||'localhost')+':8000');
const select=(key,label,options,value)=>({key,label,kind:'select',options,value});const number=(key,label,value)=>({key,label,kind:'number',value});const toggle=(key,label,value=false)=>({key,label,kind:'toggle',value});const text=(key,label,value)=>({key,label,kind:'text',value});
const groups=[
{id:'appearance',label:'Appearance',icon:'◈',fields:[select('theme','Theme',['light','dark','system'],'light'),text('accent','Accent color','#35c8d0'),select('density','UI density',['comfortable','compact'],'comfortable'),select('sidebar','Sidebar',['expanded','collapsed'],'expanded'),toggle('reduceMotion','Reduce animations'),toggle('highContrast','High contrast')]},
{id:'general',label:'General',icon:'◌',fields:[select('language','Application language',['English'],'English'),select('landing','Default landing page',['dashboard','pipeline','quantum','orchestration','evidence'],'dashboard'),number('refresh','Auto-refresh interval (seconds)',30),toggle('confirmations','Confirmation dialogs',true),toggle('rememberWorkspace','Remember last workspace',true)]},
{id:'experiments',label:'Experiments & ML',icon:'◇',fields:[select('task','Default task',['classification','regression'],'classification'),number('testSize','Default train/test split',.2),number('seed','Default random seed',42),toggle('reproducible','Reproducibility mode',true),toggle('autoRegister','Automatic experiment registration',true),toggle('saveArtifacts','Automatically save artifacts',true),number('maxDatasetMb','Maximum dataset size (MB)',25),number('maxTrainingMinutes','Maximum training time (minutes)',30)]},
{id:'quantum',label:'Quantum',icon:'Q',fields:[select('backend','Default backend',['simulator','hardware'],'simulator'),select('simulator','Quantum simulator',['aer_simulator','statevector'],'aer_simulator'),number('qubits','Default number of qubits',4),number('shots','Default shots',1024),select('noise','Noise model',['none','readout','depolarizing'],'none'),toggle('hardwareEnabled','Hardware execution'),number('maxDepth','Maximum circuit depth',80),number('resourceLimit','Quantum resource limits',8192)]},
{id:'benchmarking',label:'Benchmarking',icon:'▥',fields:[number('folds','Number of CV folds',5),number('repeats','Number of repeats',3),select('confidence','Confidence interval level',['90%','95%','99%'],'95%'),select('metrics','Metrics to calculate',['core metrics','all available metrics'],'core metrics'),select('comparison','Statistical comparison method',['paired bootstrap','permutation','descriptive only'],'descriptive only'),toggle('timing','Include training/inference time',true),toggle('circuitMetrics','Include circuit/resource metrics',true),select('conclusion','Conclusion mode',['better','comparable','worse','inconclusive'],'inconclusive')]},
{id:'privacy',label:'Privacy & Security',icon:'⌁',fields:[toggle('phiWarning','PHI/PII warning',true),toggle('sensitiveDetection','Sensitive-data detection',true),toggle('noRawLogs','Do not store raw patient records in logs',true),toggle('localOnly','Local-only processing mode',true),toggle('auditLogging','Artifact access logging',true),toggle('autoDelete','Auto-delete temporary files'),number('sessionTimeout','Session timeout (minutes)',30),toggle('datasetAudit','Dataset access audit log',true)]},
{id:'notifications',label:'Notifications',icon:'◉',fields:[toggle('notifyTraining','Training completed',true),toggle('notifyFailure','Training failed',true),toggle('notifyBenchmark','Benchmark completed',true),toggle('notifyExperiment','Experiment registered'),toggle('notifyPrediction','Prediction completed',true),toggle('notifyHardware','Hardware job status'),toggle('browserNotifications','Browser notifications')]},
{id:'storage',label:'Data & Storage',icon:'▣',fields:[text('datasetLocation','Dataset storage location','Local workspace'),text('artifactLocation','Model/artifact storage','Local workspace'),text('registryLocation','Experiment registry location','SQLite'),number('cacheSize','Cache size (MB)',512),number('retention','Artifact retention (days)',90)]},
{id:'explainability',label:'Explainability',icon:'✦',fields:[toggle('featureExplanations','Enable feature explanations',true),toggle('localExplanations','Enable local explanations',true),toggle('calibration','Show calibration information',true),toggle('uncertainty','Show uncertainty intervals',true),toggle('quantumAudit','Show quantum audit information',true),toggle('circuitDiagrams','Show circuit diagrams',true),toggle('missingEvidence','Show unsupported/missing evidence states',true)]},
{id:'prediction',label:'Prediction & Decision Support',icon:'△',fields:[number('threshold','Default probability threshold',.5),number('lowRisk','Low-risk maximum',.33),number('intermediateRisk','Intermediate-risk maximum',.66),toggle('showSensitivity','Show sensitivity/specificity',true),toggle('showErrorCounts','Show FP/FN counts',true),toggle('confirmPrediction','Require confirmation before prediction',true),toggle('researchDisclaimer','Research-only disclaimer',true)]},
{id:'advanced',label:'Advanced / Developer',icon:'⌘',fields:[text('apiEndpoint','API endpoint',API),number('apiTimeout','API timeout (ms)',15000),select('logging','Logging level',['error','warn','info','debug'],'info'),toggle('debug','Debug mode'),toggle('caching','Caching',true),toggle('experimental','Experimental features')]},
{id:'about',label:'About',icon:'ⓘ',fields:[text('version','QuantaDetect version','0.18.0'),text('entanglexVersion','EntangleX version','Phase 18'),text('phaseProgress','Phase progress','18 / 20'),text('backendVersion','Backend version','FastAPI / research mode'),text('pythonVersion','Python version','Environment supplied'),text('qiskitVersion','Qiskit version','Environment supplied'),text('license','License','Research prototype'),text('repository','GitHub repository','Itzx-kush/EntangleX-Q-Health')]}
];
const defaults=Object.fromEntries(groups.flatMap(group=>group.fields.map(field=>[field.key,field.value])));let settings={...defaults,...readSettings()};let activeGroup='appearance';function readSettings(){try{return JSON.parse(localStorage.getItem('entanglex-settings')||'{}')}catch{return {}}}const safe=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function injectStyles(){if(document.getElementById('qx-style'))return;const style=document.createElement('style');style.id='qx-style';style.textContent=`.qx-splash{position:fixed;inset:0;z-index:9999;display:grid;place-items:center;background:radial-gradient(circle at 50% 42%,#304886,#11192f 46%,#070b17);animation:qxSplash 2.3s ease forwards;pointer-events:none}.qx-splash img{width:min(430px,74vw);animation:qxLogo 1.5s cubic-bezier(.2,.8,.2,1)}.qx-splash small{position:absolute;bottom:18%;color:#c1d7ff;letter-spacing:3px;font:700 10px Arial}@keyframes qxSplash{0%,72%{opacity:1}100%{opacity:0;visibility:hidden}}@keyframes qxLogo{0%{opacity:0;transform:scale(.72);filter:blur(12px)}60%{opacity:1;transform:scale(1.04);filter:blur(0)}100%{transform:scale(1)}}.qx-brand{width:174px;height:78px;object-fit:contain}.qx-toolbar{border:1px solid var(--line);background:var(--panel2);color:var(--text);border-radius:9px;padding:8px 10px;font-size:11px;font-weight:800}.qx-nav{font-size:11px!important}.qx-nav b{margin-left:auto;font-size:10px;color:#9dc2ff}.qx-overlay{position:fixed;inset:0;z-index:80;display:grid;place-items:center;padding:20px;background:#071027b0;backdrop-filter:blur(15px);animation:qxIn .2s ease}.qx-modal{width:min(1120px,96vw);max-height:92vh;overflow:hidden;background:var(--panel);border:1px solid var(--line);border-radius:24px;box-shadow:0 30px 100px #0008;animation:qxModal .3s ease}.qx-modal-head{display:flex;justify-content:space-between;padding:24px 28px;border-bottom:1px solid var(--line);background:linear-gradient(120deg,color-mix(in srgb,var(--primary) 10%,var(--panel)),var(--panel))}.qx-modal-head h2{margin:5px 0;font-size:23px}.qx-modal-head p{margin:0;color:var(--muted);font-size:11px;max-width:650px;line-height:1.5}.qx-kicker{font-size:10px;font-weight:900;color:var(--primary);letter-spacing:1.2px}.qx-close{border:1px solid var(--line);background:var(--panel2);color:var(--text);border-radius:10px;width:36px;height:36px;font-size:20px}.qx-modal-body{display:grid;grid-template-columns:215px 1fr;min-height:440px;max-height:calc(92vh - 100px)}.qx-tabs{padding:16px;border-right:1px solid var(--line);background:var(--panel2);display:grid;align-content:start;gap:5px;overflow:auto}.qx-tab{border:1px solid transparent;background:transparent;color:var(--muted);padding:11px;border-radius:10px;text-align:left;font-weight:800;font-size:11px}.qx-tab:hover,.qx-tab.active{background:color-mix(in srgb,var(--primary) 13%,var(--panel));color:var(--text);border-color:var(--line)}.qx-tab span{display:inline-grid;width:22px;color:var(--primary)}.qx-settings-content,.qx-experience-body{padding:25px;overflow:auto}.qx-settings-content h3{margin:0 0 6px}.qx-settings-content>p{color:var(--muted);font-size:11px;margin:0 0 22px}.qx-setting-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}.qx-setting{border:1px solid var(--line);background:var(--panel2);border-radius:12px;padding:13px}.qx-setting label{display:flex;align-items:center;justify-content:space-between;gap:12px;font-size:11px;color:var(--text)}.qx-setting input,.qx-setting select{margin:0;max-width:170px;padding:8px}.qx-setting input[type=checkbox]{width:18px;height:18px;accent-color:var(--primary)}.qx-footer{display:flex;justify-content:space-between;gap:10px;margin-top:24px;padding-top:18px;border-top:1px solid var(--line)}.qx-danger,.qx-subtle{border-radius:9px;padding:10px 12px;font-weight:800;font-size:11px}.qx-danger{border:1px solid #d86a75;background:#d86a7512;color:#d86a75}.qx-subtle{border:1px solid var(--line);background:var(--panel2);color:var(--text)}.qx-experience{width:min(1180px,97vw)}.qx-center-nav{display:flex;gap:8px;margin-bottom:18px}.qx-center-nav button{border:1px solid var(--line);background:var(--panel2);color:var(--muted);padding:9px 12px;border-radius:9px;font-weight:850;font-size:11px}.qx-center-nav button.active{background:var(--primary);color:#fff}.qx-evidence-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:12px}.qx-evidence-card{border:1px solid var(--line);border-radius:13px;padding:15px;background:var(--panel2)}.qx-evidence-card small,.qx-evidence-card b{display:block}.qx-evidence-card small{color:var(--muted);font-size:10px}.qx-evidence-card b{font-size:27px;margin-top:8px}.qx-decision-layout{display:grid;grid-template-columns:1fr 1fr;gap:18px}.qx-form{display:grid;grid-template-columns:1fr 1fr;gap:12px}.qx-form label{font-size:10px;color:var(--muted);font-weight:850;text-transform:uppercase}.qx-form .full{grid-column:1/-1}.qx-form input,.qx-form select,.qx-form textarea{display:block;width:100%;margin-top:6px;border:1px solid var(--line);background:var(--panel2);color:var(--text);border-radius:8px;padding:10px;font:inherit}.qx-form textarea{min-height:96px;font-family:monospace;font-size:11px}.qx-result{margin-top:15px;border:1px solid #3eb88555;background:#3eb8850d;border-radius:13px;padding:16px}.qx-result.high{border-color:#e5645855;background:#e564580d}.qx-result-row{display:flex;justify-content:space-between;padding:7px 0;border-bottom:1px solid var(--line);font-size:11px}.qx-warning{margin-top:12px;color:#d38b34;font-size:10px;line-height:1.5}.qx-mini-list{display:grid;gap:8px;margin-top:16px}.qx-mini-list div{display:flex;justify-content:space-between;gap:10px;padding:11px;border:1px solid var(--line);border-radius:9px;font-size:10px}.qx-empty{padding:30px;text-align:center;color:var(--muted);border:1px dashed var(--line);border-radius:13px}@keyframes qxIn{from{opacity:0}to{opacity:1}}@keyframes qxModal{from{opacity:0;transform:translateY(18px) scale(.98)}to{opacity:1;transform:none}}@media(max-width:760px){.qx-modal-body,.qx-decision-layout,.qx-form{grid-template-columns:1fr}.qx-tabs{display:flex;overflow:auto;border-right:0;border-bottom:1px solid var(--line)}.qx-tab{white-space:nowrap}.qx-setting-grid{grid-template-columns:1fr}.qx-evidence-grid{grid-template-columns:1fr 1fr}}@media(prefers-reduced-motion:reduce){.qx-splash,.qx-splash img,.qx-overlay,.qx-modal{animation:none!important}}`;document.head.appendChild(style)}
function apply(){const theme=settings.theme==='system'?(matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light'):settings.theme;document.documentElement.dataset.theme=theme;localStorage.setItem('entanglex-theme',theme);document.documentElement.dataset.density=settings.density;document.documentElement.dataset.highContrast=settings.highContrast?'true':'false';document.documentElement.dataset.reduceMotion=settings.reduceMotion?'true':'false';const logo=document.querySelector('.qx-brand');if(logo)logo.src=theme==='dark'?'/assets/entanglex-logo-dark.svg':'/assets/entanglex-logo-light.svg')}
function settingsOverlay(overlay){overlay.innerHTML=`<div class="qx-modal"><div class="qx-modal-head"><div><div class="qx-kicker">ENTANGLEX / SETTINGS</div><h2>Research environment controls</h2><p>Configure appearance, reproducibility defaults and biomedical safeguards without rewriting certified evidence.</p></div><button class="qx-close">×</button></div><div class="qx-modal-body"><div class="qx-tabs">${groups.map(group=>`<button class="qx-tab" data-group="${group.id}"><span>${group.icon}</span>${group.label}</button>`).join('')}</div><div class="qx-settings-content" id="qx-settings-content"></div></div></div>`;overlay.querySelector('.qx-close').onclick=()=>overlay.remove();overlay.querySelectorAll('[data-group]').forEach(button=>button.onclick=()=>{activeGroup=button.dataset.group;renderSettings(overlay)});renderSettings(overlay)}
function renderSettings(overlay){const content=overlay.querySelector('#qx-settings-content');const group=groups.find(item=>item.id===activeGroup)||groups[0];content.innerHTML=`<h3>${group.icon} ${group.label}</h3><p>${activeGroup==='privacy'?'Biomedical-safe defaults prevent accidental protected-record exposure.':activeGroup==='quantum'?'Simulator-first defaults keep quantum experiments bounded and reproducible.':'Preferences are local to this browser and do not rewrite recorded evidence.'}</p><div class="qx-setting-grid">${group.fields.map(field=>{const value=settings[field.key]??field.value??(field.kind==='toggle'?false:'');let control='';if(field.kind==='toggle')control=`<input data-setting="${field.key}" type="checkbox" ${value?'checked':''}>`;else if(field.kind==='select')control=`<select data-setting="${field.key}">${field.options.map(option=>`<option ${String(value)===option?'selected':''}>${option}</option>`).join('')}</select>`;else control=`<input data-setting="${field.key}" type="${field.kind}" value="${safe(value)}">`;return `<div class="qx-setting"><label>${field.label}${control}</label></div>`}).join('')}</div><div class="qx-footer"><button class="qx-danger" data-reset>Reset all settings</button><button class="qx-subtle" data-close>Done</button></div>`;content.querySelectorAll('[data-setting]').forEach(element=>element.onchange=()=>{const input=element;settings[input.dataset.setting]=input.type==='checkbox'?input.checked:input.type==='number'?Number(input.value):input.value;localStorage.setItem('entanglex-settings',JSON.stringify(settings));apply()});content.querySelector('[data-reset]').onclick=()=>{settings={...defaults};localStorage.setItem('entanglex-settings',JSON.stringify(settings));apply();renderSettings(overlay)};content.querySelector('[data-close]').onclick=()=>overlay.remove()}
function openOverlay(kind){const old=document.getElementById('qx-'+kind);if(old){old.remove();return}const overlay=document.createElement('div');overlay.id='qx-'+kind;overlay.className='qx-overlay';overlay.onclick=event=>{if(event.target===overlay)overlay.remove()};if(kind==='settings')settingsOverlay(overlay);else centerOverlay(overlay);document.body.appendChild(overlay)}
function request(path,init){return fetch(API+path,init).then(async response=>{const body=await response.json();if(!response.ok)throw new Error(body.message||`Request failed (${response.status})`);return body})}
function centerOverlay(overlay){overlay.innerHTML=`<div class="qx-modal qx-experience"><div class="qx-modal-head"><div><div class="qx-kicker">PHASES 17–18 / PRODUCT CENTER</div><h2>Decision support and evidence cockpit</h2><p>Predictions use persisted model artifacts. Reports summarize recorded evidence and remain research-use outputs.</p></div><button class="qx-close">×</button></div><div class="qx-experience-body"><div class="qx-center-nav"><button class="active" data-center="prediction">△ Prediction</button><button data-center="evidence">▥ Evidence dashboard</button></div><div id="qx-center-content"></div></div></div>`;overlay.querySelector('.qx-close').onclick=()=>overlay.remove();overlay.querySelectorAll('[data-center]').forEach(button=>button.onclick=()=>{overlay.querySelectorAll('[data-center]').forEach(item=>item.classList.remove('active'));button.classList.add('active');centerContent(button.dataset.center)});document.body.appendChild(overlay);centerContent('prediction')}
async function centerContent(view){const host=document.querySelector('#qx-center-content');if(!host)return;if(view==='evidence'){host.innerHTML='<div class="qx-empty">Loading live evidence…</div>';try{const [experiments,benchmarks,reports,predictions]=await Promise.all([request('/api/experiments'),request('/api/benchmarks'),request('/api/explainability/reports'),request('/api/predictions')]);const records=[...(experiments.items||[]),...(benchmarks.items||[]),...(reports.items||[])].slice(0,10);host.innerHTML=`<div class="qx-evidence-grid"><div class="qx-evidence-card"><small>Experiments</small><b>${experiments.total??0}</b><small>Immutable registry records</small></div><div class="qx-evidence-card"><small>Benchmarks</small><b>${benchmarks.total??0}</b><small>Repeated comparison records</small></div><div class="qx-evidence-card"><small>Explainability</small><b>${reports.total??0}</b><small>Phase 15–16 reports</small></div><div class="qx-evidence-card"><small>Predictions</small><b>${predictions.total??0}</b><small>Decision-support records</small></div></div><div class="qx-mini-list">${records.length?records.map(record=>`<div><span><b>${safe(record.name||record.phase||record.target_type||record.model_family||'Evidence record')}</b><small>${safe(record.status||record.created_at||'Recorded')}</small></span><span>${safe(record.id?.slice?.(0,8)||'—')}</span></div>`).join(''):'<div class="qx-empty">No evidence records are available yet.</div>'}</div><div class="qx-footer"><button class="qx-subtle" data-export>Export live summary</button><span class="qx-warning">Evidence is descriptive; no clinical or quantum-advantage claim is made.</span></div>`;host.querySelector('[data-export]').onclick=()=>{const blob=new Blob([JSON.stringify({experiments,benchmarks,reports,predictions},null,2)],{type:'application/json'});const link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download='entanglex-phase18-evidence.json';link.click();URL.revokeObjectURL(link.href)}}catch(error){host.innerHTML=`<div class="qx-empty">Evidence could not be loaded: ${safe(error.message||'API offline')}</div>`}return}host.innerHTML=`<div class="qx-decision-layout"><div><h3>Generate a traceable prediction</h3><p class="muted">Use a persisted held-out sample or provide a matching feature vector. No input is invented.</p><div class="qx-form"><label class="full">Model run<select id="qx-model"></select></label><label>Held-out sample index<input id="qx-sample" type="number" min="0" placeholder="e.g. 0"></label><label>Probability threshold<input id="qx-threshold" type="number" min="0" max="1" step=".01" value="${settings.threshold??.5}"></label><label>Low-risk maximum<input id="qx-low" type="number" min="0" max="1" step=".01" value="${settings.lowRisk??.33}"></label><label>Intermediate maximum<input id="qx-intermediate" type="number" min="0" max="1" step=".01" value="${settings.intermediateRisk??.66}"></label><label class="full">Optional feature vector (comma-separated)<textarea id="qx-features" placeholder="Leave empty when using a held-out sample"></textarea></label></div><button class="primary accent" id="qx-predict">Generate prediction</button><div id="qx-prediction-result"></div></div><div><div class="qx-evidence-card"><small>PHASE 17 SAFEGUARDS</small><h3>Clinical boundary</h3><p class="muted">Risk bands are configurable descriptive thresholds. They are not validated clinical categories, and one record does not provide uncertainty.</p><div class="qx-bar"></div></div><div id="qx-recent-predictions" class="qx-mini-list"><div class="qx-empty">Loading prediction history…</div></div></div></div>`;try{const models=await request('/api/models/runs');const select=document.querySelector('#qx-model');select.innerHTML=(models.items||[]).map(model=>`<option value="${safe(model.id)}">${safe(model.model_type)} · ${safe(model.id.slice(0,8))}</option>`).join('')||'<option value="">No model runs</option>';const history=await request('/api/predictions');const recent=document.querySelector('#qx-recent-predictions');recent.innerHTML=(history.items||[]).slice(0,5).map(record=>`<div><span><b>${safe(record.risk_band||record.decision)}</b><small>${safe(record.input_source)}</small></span><span>${record.probability==null?'N/A':(record.probability*100).toFixed(1)+'%'}</span></div>`).join('')||'<div class="qx-empty">No predictions recorded yet.</div>';document.querySelector('#qx-predict').onclick=async()=>{const button=document.querySelector('#qx-predict');const result=document.querySelector('#qx-prediction-result');button.disabled=true;try{const raw=document.querySelector('#qx-features').value.trim();const sample=document.querySelector('#qx-sample').value.trim();const body={model_run_id:select.value,probability_threshold:Number(document.querySelector('#qx-threshold').value),low_risk_max:Number(document.querySelector('#qx-low').value),intermediate_risk_max:Number(document.querySelector('#qx-intermediate').value)};if(raw)body.features=raw.split(',').map(Number);else if(sample)body.sample_index=Number(sample);else throw new Error('Choose a held-out sample index or enter a feature vector.');const record=await request('/api/predictions',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});result.innerHTML=`<div class="qx-result ${record.risk_band==='high'?'high':''}"><h4>Prediction recorded</h4><div class="qx-result-row"><span>Decision</span><b>${safe(record.decision)}</b></div><div class="qx-result-row"><span>Probability</span><b>${record.probability==null?'Unavailable':(record.probability*100).toFixed(2)+'%'}</b></div><div class="qx-result-row"><span>Risk band</span><b>${safe(record.risk_band)}</b></div><div class="qx-result-row"><span>Source</span><b>${safe(record.input_source)}</b></div><div class="qx-warning">${record.scientific_warnings.map(warning=>safe(warning)).join('<br>')}</div></div>`}catch(error){result.innerHTML=`<div class="qx-empty">${safe(error.message||'Prediction failed')}</div>`}finally{button.disabled=false}}}catch(error){host.innerHTML=`<div class="qx-empty">Prediction center unavailable: ${safe(error.message||'API offline')}</div>`}}
function setup(){if(document.body.dataset.qxReady||!document.querySelector('.shell'))return;document.body.dataset.qxReady='true';injectStyles();const splash=document.createElement('div');splash.className='qx-splash';splash.innerHTML='<img src="/assets/entanglex-logo-light.svg" alt="EntangleX"><small>QUANTUM HEALTH RESEARCH</small>';document.body.appendChild(splash);const logo=document.querySelector('.logo');if(logo)logo.innerHTML='<img class="qx-brand" src="/assets/entanglex-logo-light.svg" alt="EntangleX Q-Health">';const phase=document.querySelector('.phase');phase?.querySelector('b')?.replaceChildren('Phase 18 of 20');const progress=phase?.querySelector('em');if(progress)progress.style.width='90%';phase?.querySelector('small')?.replaceChildren('Decision support + evidence cockpit');const actions=document.querySelector('.headActions');if(actions){const settingsButton=document.createElement('button');settingsButton.className='qx-toolbar';settingsButton.textContent='⚙ Settings';settingsButton.onclick=()=>openOverlay('settings');const centerButton=document.createElement('button');centerButton.className='qx-toolbar';centerButton.textContent='△ Decision center';centerButton.onclick=()=>openOverlay('center');actions.insertBefore(centerButton,actions.firstChild);actions.insertBefore(settingsButton,actions.firstChild)}const nav=document.querySelector('.sidebar nav');if(nav){const center=document.createElement('button');center.type='button';center.className='qx-nav';center.innerHTML='<span>△</span> Decision center <b>17–18</b>';center.onclick=()=>openOverlay('center');nav.appendChild(center);const setting=document.createElement('button');setting.type='button';setting.className='qx-nav';setting.innerHTML='<span>⚙</span> Settings';setting.onclick=()=>openOverlay('settings');nav.appendChild(setting)}apply()}
const observer=new MutationObserver(setup);observer.observe(document.documentElement,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',setup);else setTimeout(setup,0);

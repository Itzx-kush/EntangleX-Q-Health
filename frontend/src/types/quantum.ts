export type QuantumRuntimeStatus={simulator_available:boolean;qiskit_available:boolean;ibm_runtime_available:boolean;ibm_token_configured:boolean;real_quantum_enabled:boolean;provider_ready:boolean;default_backend:string;default_qubits:number;max_qubits:number;max_shots:number};
export type QuantumDiagnosticConfig={backend:"aer_simulator"|"ibm_quantum";qubits:number;shots:number;seed:number};
export type QuantumDiagnosticRun={id:string;backend:string;execution_mode:string;qubits:number;shots:number;seed:number;circuit_name:string;circuit_depth:number;circuit_size:number;operation_counts:Record<string,number>;measurement_counts:Record<string,number>;execution_duration_seconds:number;artifact_path:string;created_at:string};

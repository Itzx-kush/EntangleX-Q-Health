// @ts-nocheck
const API=(globalThis as any).ENTANGLEX_API_URL||`http://${location.hostname||'localhost'}:8000`;
const MODELS=[['logistic_regression','Logistic Regression','Classical'],['svm','Support Vector Machine','Classical'],['random_forest','Random Forest','Classical'],['vqc','VQC','Hybrid quantum'],['qsvm','Fidelity QSVM','Hybrid quantum'],['qnn','QNN','Hybrid quantum']];
const terminal=new Set(['succeeded','failed','cancelled','partial','interrupted']);
const esc=(value:any)=>String(value??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const pretty=(value:string)=>String(value||'').replaceAll('_',' ').replace(/\b\w/g,c=>c.toUpperCase());
async function request(path:string,init?:RequestInit){const response=await fetch(API+path,init);const body=await response.json().catch(()=>null);if(!response.ok)throw new Error(body?.message||body?.detail||`Request failed: ${response.status}`);return body}
function styles(){if(document.querySelector('#controlled-experiment-styles'))return;const s=document.createElement('style');s.id='controlled-experiment-styles';s.textContent=`
.experimentNav{border:1px solid color-mix(in srgb,var(--accent) 25%,var(--border));background:color-mix(in srgb,var(--accent) 8%,var(--surface));color:var(--text);font-weight:750}.experimentNav .navIcon{background:var(--accent);color:white}
.ceOverlay{position:fixed;inset:0;z-index:120;background:rgba(9,15,28,.72);backdrop-filter:blur(8px);padding:24px;overflow:auto}.ceShell{width:min(1180px,100%);min-height:calc(100vh - 48px);margin:auto;background:var(--surface);color:var(--text);border:1px solid var(--border);border-radius:14px;box-shadow:0 24px 80px rgba(0,0,0,.24)}.ceHead{display:flex;justify-content:space-between;gap:24px;padding:28px 32px;border-bottom:1px solid var(--border)}.ceHead h1{margin:5px 0 6px;font-size:30px}.ceHead p{margin:0;color:var(--text-muted);max-width:720px}.ceClose{width:44px;height:44px;border:1px solid var(--border);border-radius:8px;background:var(--surface-raised);color:var(--text);font-size:22px}.ceBody{padding:28px 32px;display:grid;gap:28px}.ceKicker{font-size:12px;letter-spacing:.1em;font-weight:800;color:var(--accent)}.ceSteps{display:grid;grid-template-columns:repeat(6,1fr);border:1px solid var(--border);border-radius:10px;overflow:hidden}.ceSteps span{padding:12px;border-right:1px solid var(--border);font-size:12px;font-weight:700}.ceSteps span:last-child{border:0}.ceGrid{display:grid;grid-template-columns:minmax(0,1.05fr) minmax(360px,.95fr);gap:24px}.ceSection{border-top:1px solid var(--border);padding-top:22px}.ceSection:first-child{border-top:0;padding-top:0}.ceSection h2{font-size:20px;margin:0 0 6px}.ceSection>p{color:var(--text-muted);margin:0 0 16px}.ceField{display:grid;gap:7px;margin-bottom:14px;font-size:13px;font-weight:700}.ceField input,.ceField select{min-height:44px;padding:9px 11px;border:1px solid var(--border);border-radius:8px;background:var(--surface-raised);color:var(--text)}.ceModels{display:grid;grid-template-columns:1fr 1fr;gap:10px}.ceModel{display:flex;align-items:flex-start;gap:10px;border:1px solid var(--border);padding:13px;border-radius:9px;background:var(--surface-raised);min-height:68px}.ceModel:has(input:checked){border-color:var(--accent);background:color-mix(in srgb,var(--accent) 8%,var(--surface))}.ceModel input{margin-top:3px}.ceModel b,.ceModel small{display:block}.ceModel small{color:var(--text-muted);margin-top:3px}.ceQuantum{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-top:14px}.ceActions{display:flex;gap:10px;flex-wrap:wrap}.ceButton{min-height:44px;padding:0 16px;border:1px solid var(--border);border-radius:8px;background:var(--surface-raised);color:var(--text);font-weight:750}.ceButton.primary{background:var(--accent);border-color:var(--accent);color:white}.ceButton.danger{color:var(--error);border-color:color-mix(in srgb,var(--error) 40%,var(--border))}.ceButton:disabled{opacity:.5}.ceHealth{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.ceHealth div{padding:13px;border:1px solid var(--border);border-radius:9px}.ceHealth small,.ceHealth b{display:block}.ceHealth small{color:var(--text-muted);margin-bottom:5px}.ceCallout{padding:13px 15px;border-left:3px solid var(--accent);background:color-mix(in srgb,var(--accent) 7%,var(--surface));font-size:13px;line-height:1.5}.ceError{border-left-color:var(--error);color:var(--error)}.ceJobs{display:grid;gap:10px}.ceJob{border:1px solid var(--border);border-radius:9px;padding:14px;background:var(--surface-raised)}.ceJobHead{display:flex;justify-content:space-between;gap:10px}.ceJob small{color:var(--text-muted)}.ceStage{margin-top:9px;display:flex;justify-content:space-between;font-size:12px}.ceMeter{height:4px;background:var(--border);margin-top:8px}.ceMeter i{display:block;height:100%;background:var(--accent)}.ceTableWrap{overflow:auto;border:1px solid var(--border);border-radius:9px}.ceTable{width:100%;border-collapse:collapse;min-width:760px}.ceTable th,.ceTable td{text-align:left;padding:11px 12px;border-bottom:1px solid var(--border);font-size:12px}.ceTable th{color:var(--text-muted);background:var(--surface-raised)}.ceClassical{border-left:3px solid #2783de}.ceQuantumRun{border-left:3px solid #8b5cf6}.ceCircuit{margin-top:10px}.ceCircuit summary{cursor:pointer;font-weight:700}.ceCircuit pre{white-space:pre;overflow:auto;background:#111827;color:#e5e7eb;border-radius:8px;padding:12px;font-size:11px}.ceFingerprint{font:12px ui-monospace,monospace;word-break:break-all;color:var(--text-muted)}
@media(max-width:820px){.ceOverlay{padding:0}.ceShell{border-radius:0;min-height:100vh}.ceHead,.ceBody{padding:20px}.ceGrid{grid-template-columns:1fr}.ceSteps{grid-template-columns:repeat(2,1fr)}.ceModels,.ceQuantum,.ceHealth{grid-template-columns:1fr}.ceHead h1{font-size:25px}}
@media(prefers-reduced-motion:reduce){.ceOverlay *{scroll-behavior:auto!important}}
`;document.head.appendChild(s)}
let overlay:HTMLElement|null=null;let poll:number|undefined;let current:any=null;
function close(){if(poll)clearTimeout(poll);overlay?.remove();overlay=null}
async function open(){if(overlay)return close();styles();overlay=document.createElement('div');overlay.className='ceOverlay';overlay.innerHTML=`<div class="ceShell" role="dialog" aria-modal="true" aria-labelledby="ce-title"><header class="ceHead"><div><span class="ceKicker">CONTROLLED EXPERIMENT WORKSPACE</span><h1 id="ce-title">One experiment. Shared conditions. Honest evidence.</h1><p>Choose classical and quantum models together, execute them asynchronously against one persisted preprocessing split, and compare only measured outputs.</p></div><button class="ceClose" aria-label="Close">×</button></header><main class="ceBody"><div class="ceSteps"><span>1 · Dataset</span><span>2 · Prepare</span><span>3 · Choose models</span><span>4 · Execute</span><span>5 · Compare</span><span>6 · Explain / Predict</span></div><div class="ceGrid"><div id="ce-config"></div><div id="ce-runtime"></div></div><section id="ce-active"></section><section id="ce-results"></section><section id="ce-recent"></section></main></div>`;overlay.onclick=e=>{if(e.target===overlay)close()};overlay.querySelector('.ceClose')!.onclick=close;document.body.appendChild(overlay);await renderInitial()}
async function renderInitial(){const config=overlay!.querySelector('#ce-config')!;const runtime=overlay!.querySelector('#ce-runtime')!;config.innerHTML='<div class="ceCallout">Loading persisted preprocessing runs…</div>';runtime.innerHTML='<div class="ceCallout">Running environment diagnostics…</div>';try{const [preps,diagnostics,experiments]=await Promise.all([request('/api/preprocessing/runs'),request('/api/orchestration/runtime-diagnostics'),request('/api/orchestration/controlled-experiments')]);renderConfig(preps.items||[]);renderRuntime(diagnostics);renderRecent(experiments.items||[])}catch(error){config.innerHTML=`<div class="ceCallout ceError">${esc(error.message||'Experiment workspace unavailable')}</div>`;runtime.innerHTML='<div class="ceCallout">Start the FastAPI backend and retry.</div>'}}
function renderConfig(preps:any[]){const host=overlay!.querySelector('#ce-config')!;host.innerHTML=`<section class="ceSection"><h2>Create controlled experiment</h2><p>All selected estimators share the exact persisted dataset, split, preprocessing, feature selection and reduction artifact.</p><label class="ceField">Experiment name<input id="ce-name" value="Controlled biomedical comparison"></label><label class="ceField">Prepared dataset / shared split<select id="ce-prep">${preps.map(p=>`<option value="${esc(p.id)}">${esc(p.dataset_id.slice(0,8))} · ${p.output_features} features · seed ${p.configuration.random_seed}</option>`).join('')||'<option value="">No preprocessing runs available</option>'}</select></label><div class="ceModels">${MODELS.map(([id,label,family],index)=>`<label class="ceModel"><input type="checkbox" value="${id}" ${index===0||index===3?'checked':''}><span><b>${label}</b><small>${family}</small></span></label>`).join('')}</div><div class="ceQuantum"><label class="ceField">Qubits<input id="ce-qubits" type="number" min="1" max="8" value="4"></label><label class="ceField">Entanglement<select id="ce-entanglement"><option>linear</option><option>ring</option></select></label><label class="ceField">Seed<input id="ce-seed" type="number" value="42"></label></div><div class="ceCallout">Quantum selections automatically create one shared angle-encoding artifact. Current QML execution is labelled exact local statevector simulation—not hardware.</div><div class="ceActions"><button id="ce-create" class="ceButton primary" ${preps.length?'':'disabled'}>Create and run experiment</button><button id="ce-refresh" class="ceButton">Refresh</button></div></section>`;host.querySelector('#ce-create')!.onclick=create;host.querySelector('#ce-refresh')!.onclick=renderInitial}
function renderRuntime(d:any){const q=d.quantum||{};const host=overlay!.querySelector('#ce-runtime')!;host.innerHTML=`<section class="ceSection"><h2>Runtime verification</h2><p>Availability and executed verification are reported separately.</p><div class="ceHealth"><div><small>Backend</small><b>${d.database?.available?'AVAILABLE':'UNAVAILABLE'}</b></div><div><small>Qiskit</small><b>${q.availability?.toUpperCase()||'UNKNOWN'}</b></div><div><small>Quantum smoke</small><b>${q.smoke_test?.verified?'VERIFIED':pretty(q.smoke_test?.status||'unavailable')}</b></div></div><div class="ceCallout">Execution mode: <b>${esc(q.execution_mode||'unavailable')}</b><br>Hardware verified: <b>${q.hardware_verified?'YES':'NO'}</b></div></section>`}
async function create(){const button=overlay!.querySelector('#ce-create') as HTMLButtonElement;const models=[...overlay!.querySelectorAll('.ceModel input:checked')].map((x:any)=>x.value);if(!models.length)return showError('Select at least one model.');const preprocessing_run_id=(overlay!.querySelector('#ce-prep') as HTMLSelectElement).value;if(!preprocessing_run_id)return showError('Run preprocessing first.');button.disabled=true;button.textContent='Creating…';try{current=await request('/api/orchestration/controlled-experiments',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:(overlay!.querySelector('#ce-name') as HTMLInputElement).value,preprocessing_run_id,models,quantum:{qubits:Number((overlay!.querySelector('#ce-qubits') as HTMLInputElement).value),entanglement:(overlay!.querySelector('#ce-entanglement') as HTMLSelectElement).value,seed:Number((overlay!.querySelector('#ce-seed') as HTMLInputElement).value)}})});renderExperiment(current);schedule()}catch(error){showError(error.message||'Experiment creation failed')}finally{button.disabled=false;button.textContent='Create and run experiment'}}
function showError(message:string){overlay!.querySelector('#ce-active')!.innerHTML=`<div class="ceCallout ceError" role="alert">${esc(message)}</div>`}
function schedule(){if(!current||terminal.has(current.status))return;poll=window.setTimeout(async()=>{try{current=await request(`/api/orchestration/controlled-experiments/${current.id}`);renderExperiment(current);schedule()}catch(error){showError(error.message)}},1300)}
function renderExperiment(exp:any){const active=overlay!.querySelector('#ce-active')!;active.innerHTML=`<section class="ceSection"><span class="ceKicker">ACTIVE EXPERIMENT</span><h2>${esc(exp.name)} · ${pretty(exp.status)}</h2><p class="ceFingerprint">Experiment ${esc(exp.id)}<br>Comparison fingerprint ${esc(exp.comparison_fingerprint)}</p><div class="ceJobs">${exp.jobs.map((job:any)=>`<article class="ceJob ${MODELS.find(x=>x[0]===job.model_type)?.[2]==='Classical'?'ceClassical':'ceQuantumRun'}"><div class="ceJobHead"><div><b>${pretty(job.model_type)}</b><br><small>${esc(job.id)}</small></div><b>${pretty(job.status)}</b></div><div class="ceStage"><span>${pretty(job.stage)}</span><span>Verified checkpoint ${job.checkpoint} / ${job.total_checkpoints}</span></div><div class="ceMeter" aria-label="${job.checkpoint} of ${job.total_checkpoints} completed checkpoints"><i style="width:${job.checkpoint/job.total_checkpoints*100}%"></i></div>${job.error?`<div class="ceCallout ceError">${esc(job.error.code)} · ${esc(job.error.message)}</div>`:''}</article>`).join('')}</div><div class="ceActions"><button id="ce-cancel" class="ceButton danger" ${terminal.has(exp.status)?'disabled':''}>Request cancellation</button><button id="ce-rerun" class="ceButton" ${terminal.has(exp.status)?'':'disabled'}>Rerun as new experiment</button></div></section>`;active.querySelector('#ce-cancel')!.onclick=async()=>{current=await request(`/api/orchestration/controlled-experiments/${exp.id}/cancel`,{method:'POST'});renderExperiment(current)};active.querySelector('#ce-rerun')!.onclick=async()=>{current=await request(`/api/orchestration/controlled-experiments/${exp.id}/rerun`,{method:'POST'});renderExperiment(current);schedule()};renderResults(exp)}
const metric=(job:any,key:string)=>{const value=job.result?.metrics?.[key];return typeof value==='number'?(key.includes('duration')?`${value.toFixed(3)} s`:`${(value*100).toFixed(2)}%`):'NA'};
function renderResults(exp:any){const jobs=exp.jobs.filter((job:any)=>job.status==='succeeded');const host=overlay!.querySelector('#ce-results')!;if(!jobs.length){host.innerHTML='<section class="ceSection"><h2>Measured comparison</h2><div class="ceCallout">Results appear only after a model has actually completed. Missing metrics remain NA.</div></section>';return}host.innerHTML=`<section class="ceSection"><h2>Classical vs hybrid quantum</h2><p>Shared fingerprint: ${esc(exp.comparison_fingerprint.slice(0,16))}… · Scope: identical persisted held-out partition · CV: not executed by this runner.</p><div class="ceTableWrap"><table class="ceTable"><thead><tr><th>Model</th><th>Execution</th><th>Accuracy</th><th>Balanced accuracy</th><th>F1</th><th>Sensitivity</th><th>Specificity</th><th>ROC-AUC</th><th>Training</th></tr></thead><tbody>${jobs.map((j:any)=>`<tr><td><b>${pretty(j.model_type)}</b></td><td>${esc(j.result.execution.mode)}</td><td>${metric(j,'accuracy')}</td><td>${metric(j,'balanced_accuracy')}</td><td>${metric(j,'f1')}</td><td>${metric(j,'sensitivity')}</td><td>${metric(j,'specificity')}</td><td>${metric(j,'roc_auc')}</td><td>${typeof j.result.training_duration_seconds==='number'?j.result.training_duration_seconds.toFixed(3)+' s':'NA'}</td></tr><tr><td colspan="9">Artifact: ${j.result.artifact_integrity?.verified?'SHA-256 verified':'not verified'}${j.result.circuit?`<details class="ceCircuit"><summary>Inspect actual ${pretty(j.model_type)} circuit description</summary><p>${esc(j.result.circuit.execution_evidence)}</p><pre>${esc(j.result.circuit.diagram)}</pre></details>`:''}</td></tr>`).join('')}</tbody></table></div><div class="ceCallout">No winner is inferred automatically. A valid result may favor classical, favor quantum, be mixed, or remain inconclusive.</div></section>`}
function renderRecent(items:any[]){const host=overlay!.querySelector('#ce-recent')!;host.innerHTML=`<section class="ceSection"><h2>Recent controlled experiments</h2><div class="ceJobs">${items.slice(0,6).map(exp=>`<button class="ceJob" data-id="${esc(exp.id)}"><div class="ceJobHead"><b>${esc(exp.name)}</b><span>${pretty(exp.status)}</span></div><small>${esc(exp.comparison_fingerprint.slice(0,20))}… · ${exp.summary.succeeded}/${exp.summary.selected} succeeded</small></button>`).join('')||'<div class="ceCallout">No controlled experiments recorded.</div>'}</div></section>`;host.querySelectorAll('[data-id]').forEach((button:any)=>button.onclick=async()=>{current=await request(`/api/orchestration/controlled-experiments/${button.dataset.id}`);renderExperiment(current);schedule()})}
function setup(){if(document.querySelector('.experimentNav'))return;const nav=document.querySelector('.primaryNav');if(!nav)return;const button=document.createElement('button');button.type='button';button.className='navItem experimentNav';button.innerHTML='<span class="navIcon">EX</span><span>Experiments</span>';button.onclick=open;nav.prepend(button)}
const observer=new MutationObserver(setup);observer.observe(document.documentElement,{childList:true,subtree:true});if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',setup);else setup();

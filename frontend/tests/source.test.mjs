import test from'node:test';import assert from'node:assert/strict';import{readFileSync}from'node:fs';const source=readFileSync(new URL('../src/App.tsx',import.meta.url),'utf8'),api=readFileSync(new URL('../src/services/api.ts',import.meta.url),'utf8');test('medical disclaimer',()=>assert.match(source,/not a substitute for professional medical diagnosis/));test('no quantum advantage claim',()=>assert.match(source,/No assumed quantum advantage/));test('dataset API',()=>assert.match(api,/api\/datasets\/upload/));test('preprocessing API',()=>assert.match(api,/api\/preprocessing\/run/));test('class balancing',()=>assert.match(source,/random_over/));test('feature reduction',()=>assert.match(source,/mutual_info/));test('classical baselines',()=>{assert.match(source,/Logistic Regression/);assert.match(source,/Support Vector Machine/);assert.match(source,/Random Forest/);assert.match(source,/Held-out test rows are preserved for Phase 7/);assert.match(api,/api\/models\/train/)})

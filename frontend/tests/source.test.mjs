import test from "node:test";import assert from "node:assert/strict";import{readFileSync}from"node:fs";const source=readFileSync(new URL("../src/App.tsx",import.meta.url),"utf8"),api=readFileSync(new URL("../src/services/api.ts",import.meta.url),"utf8");test("medical disclaimer is present",()=>assert.match(source,/not a substitute for professional medical diagnosis/));test("quantum advantage is not claimed",()=>assert.match(source,/No assumed quantum advantage/));test("no demo metrics are hardcoded",()=>["0.94","92.1%","88.7%"].forEach(v=>assert.equal(source.includes(v),false)));test("dataset upload and target APIs are integrated",()=>{assert.match(api,/api\/datasets\/upload/);assert.match(source,/Choose CSV or XLSX/)});test("preprocessing is integrated without metrics",()=>assert.match(api,/api\/preprocessing\/run/));test("class balancing is training-only",()=>assert.match(source,/Random oversampling/));test("feature reduction is training-only",()=>{assert.match(source,/Variance threshold/);assert.match(source,/Mutual information/);assert.match(source,/PCA/);assert.match(source,/feature reduction on train only/)});
